# Builds the Go `console` CLI and `console-server-go` agent server for every
# platform on each push to main, then uploads them to a rolling GitHub Release
# tagged `console-server` (latest always wins).
#
# Assets per platform: console-<suffix> + console-server-go-<suffix> +
# libfff_c-<suffix>.{so,dylib} sidecar (fff file-search native lib, dlopened at
# runtime — same assets as apps/server-go/scripts/fetch-fff-lib.sh, which only
# covers the host). `console start` launches the sibling console-server-go
# binary, so install/upgrade must place both binaries side by side.
name: Build console

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - os-suffix: linux-x64
            runner: ubuntu-latest
            goos: linux
            goarch: amd64
            fff-asset: c-lib-x86_64-unknown-linux-gnu.so
            lib-asset: libfff_c-linux-x64.so
          - os-suffix: macos-x64
            runner: macos-14
            goos: darwin
            goarch: amd64
            fff-asset: c-lib-x86_64-apple-darwin.dylib
            lib-asset: libfff_c-macos-x64.dylib
          - os-suffix: macos-arm64
            runner: macos-14
            goos: darwin
            goarch: arm64
            fff-asset: c-lib-aarch64-apple-darwin.dylib
            lib-asset: libfff_c-macos-arm64.dylib
    steps:
      - uses: actions/checkout@v4

      # Must match the `go` directive in apps/*/go.mod (source of truth).
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: |
            apps/server-go/go.sum
            apps/cli-go/go.sum

      # server-go uses cgo (fff dlopen bindings); cli-go is pure Go.
      # Flags: -trimpath for reproducible builds; -s -w strips debug info.
      - name: Build console CLI
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 1
        run: >
          go -C apps/cli-go build -trimpath -ldflags="-s -w"
          -o ../../console-${{ matrix.os-suffix }} ./cmd/console

      - name: Build console-server-go
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 1
        run: >
          go -C apps/server-go build -trimpath -ldflags="-s -w"
          -o ../../console-server-go-${{ matrix.os-suffix }} ./cmd/server

      # Fetch the target platform's libfff_c from fff releases
      # (fetch-fff-lib.sh only covers the runner's own platform).
      - name: Stage fff native sidecar
        run: |
          curl -fL "https://github.com/dmtrKovalenko/fff/releases/latest/download/${{ matrix.fff-asset }}" \
            -o "${{ matrix.lib-asset }}"

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: console-${{ matrix.os-suffix }}
          path: |
            console-${{ matrix.os-suffix }}
            console-server-go-${{ matrix.os-suffix }}
            ${{ matrix.lib-asset }}
          if-no-files-found: error

  # Single publisher: the matrix jobs previously uploaded to the same
  # rolling release in parallel, racing inside action-gh-release. Builds
  # stay parallel; only this job mutates the release.
  publish:
    needs: build
    runs-on: ubuntu-latest

    # Prevent overlapping pushes from modifying the rolling release together.
    concurrency:
      group: console-server-release
      cancel-in-progress: true

    steps:
      - name: Download packages
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      # Retried: the upload itself usually succeeds but the action's final
      # release API call occasionally times out (Headers Timeout Error).
      # Re-runs are safe — assets are deleted then re-uploaded.
      - name: Publish rolling release
        uses: Wandalen/wretry.action@v3
        with:
          attempt_limit: 3
          attempt_delay: 15000
          action: softprops/action-gh-release@v2
          with: |
            tag_name: console-server
            name: console-server (latest)
            prerelease: false
            make_latest: true
            files: |
              dist/console-linux-x64
              dist/console-server-go-linux-x64
              dist/libfff_c-linux-x64.so
              dist/console-macos-x64
              dist/console-server-go-macos-x64
              dist/libfff_c-macos-x64.dylib
              dist/console-macos-arm64
              dist/console-server-go-macos-arm64
              dist/libfff_c-macos-arm64.dylib
